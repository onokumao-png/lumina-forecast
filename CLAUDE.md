# CLAUDE.md — Project Instructions

## Memory

Always read `Memory.MD` at the start of each session and apply the preferences there.

When the user expresses a new preference (communication style, code style, workflow habit, tool choice, etc.), update `Memory.MD` immediately — add it under the relevant section and update the "Last updated" date. Do not wait until the end of the session.

## Project
- Python project: entry point `app.py`, launched via `run.sh`
